global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: api
    dns_sd_configs:
      - names:
          - api
        type: A
        port: 5000

  - job_name: "postgres-exporter"
    static_configs:
      - targets: ["postgres-exporter:9187"]
